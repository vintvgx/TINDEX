/** Shared display label for ExitManager's runner_mode — see exit_manager.py's
 *  runner_mode handling for what each value actually does. */
export const RUNNER_MODE_LABEL: Record<'trail' | 'be_hold' | 'none', string> = {
  trail: 'Trail', be_hold: 'BE Hold', none: 'No Trail',
};
